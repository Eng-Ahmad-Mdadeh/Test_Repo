name: neurobalance_app
description: منصة تقييم نفسي مبنية بـ Flutter تعتمد على Clean Architecture.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  collection: ^1.17.2

dev_dependencies:
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
